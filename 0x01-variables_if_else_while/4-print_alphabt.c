#include <stdio.h>
#include <stdlib.h>
#include <time.h>


/**
 * main - Entry Point
 * Return: Always 0 (Success)
 */


int msin(void)



{
	char low, e, q;
	e = 'e';
	q = 'q';
        for (low = 'a' ; low <= 'z' ; low++)
	{
	if (low != e && low != q)
	}
        putchar('\n');
	return (0);
