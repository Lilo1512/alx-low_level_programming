#include "main.h"
 /**
  *  _isupper - chek if a letter is upper
  *  @x: The number to be checked
  *  Return: 1 for upper letter or 0 for else
  */

int _isdigit(int x)

{

        if (x >= 48 && x <= 57)

                return (1);
        else
                return (0);
}
