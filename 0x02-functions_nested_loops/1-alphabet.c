#include "main.h"

/**
 * print_alphabet - Make the alphabet
 * Return: void
 */

void print_alphabet (void)
{
        putchar (c);
        for (c = 'a'; c <= 'z' ; c++)

{
        putchar(c);
}
        putchar('\n');
	}
